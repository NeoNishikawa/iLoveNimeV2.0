import { api } from "./api.js";
import { loadTracking, saveTracking, upsertTracking, clearTracking, exportTracking, importTracking } from "./storage.js";
import { renderGenres, renderCatalog, renderDetail, renderStats, renderCollectionFilters, filterCollection, renderCollection } from "./view.js";
import { reveal, revealNewAnime, switchSearch, showDetail, menu, savedFlight, smoothScroll } from "./motion.js";
import { initPlayer, openPlayer } from "./player.js";
import { initScene } from "./scene.js";
import { initScrollFade } from "./scroll-fade.js";

const PAGE_SIZE = 20;
const $ = (value) => document.querySelector(value);
const state = { query: "", genre: "", slides: [], slide: 0, detail: null, tracking: loadTracking(), collectionFilter: "all", status: "planned", progress: 0, moved: false, searchToken: 0 };

function renderTracking() {
  const visibleItems = filterCollection(state.tracking, state.collectionFilter);
  $("#collectionStats").innerHTML = renderStats(state.tracking);
  $("#collectionFilters").innerHTML = renderCollectionFilters(state.tracking, state.collectionFilter);
  $("#collectionList").innerHTML = renderCollection(visibleItems);
  document.querySelectorAll("[data-collection-filter]").forEach((button) => button.onclick = () => { state.collectionFilter = button.dataset.collectionFilter; renderTracking(); });
  document.querySelectorAll("[data-open]").forEach((button) => button.onclick = () => selectAnime(button.dataset.open));
}
function activateSearch() { return; }
function updateSlideControls() { const total = Math.max(state.slides.length, 1); [$("#slideLabel"), $("#slideLabelBottom")].forEach((label) => { if (label) label.textContent = `${String(state.slide + 1).padStart(2, "0")} / ${String(total).padStart(2, "0")}`; }); [$("#previousSlide"), $("#previousSlideBottom")].forEach((button) => { if (button) button.disabled = state.slide === 0; }); [$("#nextSlide"), $("#nextSlideBottom")].forEach((button) => { if (button) button.disabled = state.slide >= state.slides.length - 1; }); }
function renderSlide() { const current = state.slides[state.slide] || []; $("#catalogGrid").innerHTML = renderCatalog(current, state.slide * PAGE_SIZE); updateSlideControls(); document.querySelectorAll(".anime-card").forEach((card) => card.onclick = () => selectAnime(card.dataset.slug)); const cards = document.querySelectorAll(".anime-card"); if (!state.query && !state.genre) revealNewAnime(cards); else reveal(cards); }
function setSlide(next) { state.slide = Math.max(0, Math.min(state.slides.length - 1, next)); renderSlide(); smoothScroll($("#discover")); }
function escapeHtml(value) { const node = document.createElement("span"); node.textContent = String(value || ""); return node.innerHTML; }
function downloadTracking() { const blob = new Blob([exportTracking(state.tracking)], { type: "application/json" }); const url = URL.createObjectURL(blob); const link = document.createElement("a"); link.href = url; link.download = `ilovenime-tracking-${new Date().toISOString().slice(0, 10)}.json`; link.click(); setTimeout(() => URL.revokeObjectURL(url), 0); }
function importTrackingFile(file) { if (!file) return; const loader = $("#dataLoader"); const loaderText = $("#dataLoaderText"); loader?.classList.remove("hidden"); if (loaderText) loaderText.textContent = "Membaca file Local Storage…"; const reader = new FileReader(); reader.onprogress = (event) => { if (loaderText && event.lengthComputable) loaderText.textContent = `Membaca data… ${Math.round(event.loaded / event.total * 100)}%`; }; reader.onload = () => { window.setTimeout(() => { try { if (loaderText) loaderText.textContent = "Menormalisasi daftar anime…"; state.tracking = importTracking(reader.result); state.collectionFilter = "all"; renderTracking(); if (loaderText) loaderText.textContent = `${state.tracking.length} judul berhasil dipulihkan`; window.setTimeout(() => loader?.classList.add("hidden"), 500); } catch (error) { loader?.classList.add("hidden"); window.alert(`Import gagal: ${error.message}`); } }, 280); }; reader.onerror = () => { loader?.classList.add("hidden"); window.alert("Import gagal: file tidak dapat dibaca."); }; reader.readAsText(file); }
function applyCatalog(response, heading, message) { state.slides = response.slides || []; state.slide = 0; $("#resultCount").textContent = response.total || 0; $("#resultsHeading").innerHTML = heading; $("#catalogState").textContent = message; $("#slider").classList.toggle("hidden", !response.total); if (response.total) renderSlide(); }
async function loadDaily() { const token = ++state.searchToken; $("#catalogState").textContent = "Membaca jadwal anime hari ini…"; $("#slider").classList.add("hidden"); try { const response = await api.daily(); if (token !== state.searchToken) return; const label = response.label || "Anime terbaru hari ini"; applyCatalog(response, `New <span>anime.</span>`, response.total ? `${label} · gunakan tombol panah atas atau bawah untuk melihat semua slide.${response.stale ? " Source lambat; menampilkan data terakhir yang tersedia." : ""}` : `${label} tidak memiliki judul baru pada source.`); } catch (error) { if (token === state.searchToken) { $("#resultCount").textContent = "0"; $("#catalogState").textContent = error.message; } } }
async function search() { if (!state.query && !state.genre) { api.cancelCatalog(); return loadDaily(); } const token = ++state.searchToken; $("#catalogState").textContent = "Mencari saat Anda mengetik…"; $("#slider").classList.add("hidden"); try { const response = await api.catalog(state.query, state.genre); if (token !== state.searchToken) return; const aliasNote = response.aliasUsed ? ` Ditemukan melalui alias: ${response.aliasUsed}.` : ""; applyCatalog(response, state.query ? `Hasil untuk <span>“${escapeHtml(state.query)}”</span>` : "Hasil berdasarkan <span>genre.</span>", response.total ? `Gunakan tombol panah atas atau bawah; item dengan nama yang sama tetap tersedia jika memiliki slug berbeda.${aliasNote}${response.partial ? " Sebagian halaman belum dimuat karena source lambat." : ""}` : "Tidak ada hasil. Coba kata kunci lain."); } catch (error) { if (token === state.searchToken) { $("#catalogState").textContent = error.message; $("#resultCount").textContent = "0"; } } }
function persistCurrent(watchedEpisodes = []) { const current = state.tracking.find((item) => item.slug === state.detail.slug); state.tracking = upsertTracking(state.tracking, { slug: state.detail.slug, title: state.detail.title, image: state.detail.image, total: state.detail.episodes.length, status: state.status, progress: state.progress, watchedEpisodes: watchedEpisodes.length ? watchedEpisodes : (current?.watchedEpisodes || []) }); saveTracking(state.tracking); renderTracking(); }
function markWatchedAndGetNext(episode) { if (!state.detail || !episode) return null; const number = Number(episode.number); const existing = state.tracking.find((item) => item.slug === state.detail.slug); const watched = Array.from(new Set([...(existing?.watchedEpisodes || []), number])).sort((a, b) => a - b); state.progress = Math.max(state.progress, number); state.tracking = upsertTracking(state.tracking, { slug: state.detail.slug, title: state.detail.title, image: state.detail.image, total: state.detail.episodes.length, status: state.status, progress: state.progress, watchedEpisodes: watched }); const current = state.tracking.find((item) => item.slug === state.detail.slug); state.status = current.status; state.progress = current.progress; saveTracking(state.tracking); renderTracking(); $("#detailBody").innerHTML = renderDetail(state.detail, current); bindDetail(); const index = state.detail.episodes.findIndex((item) => Number(item.number) === number); return state.detail.episodes[index + 1] || null; }
async function selectAnime(slug) { $("#detail").classList.remove("hidden"); $("#detailBody").innerHTML = '<p class="empty-state" style="padding:34px">Membaca detail dari YAOI API…</p>'; smoothScroll($("#detail")); try { const response = await api.detail(slug); state.detail = response.data; const current = state.tracking.find((item) => item.slug === slug); state.status = current?.status || "planned"; state.progress = current?.progress || 0; $("#detailBody").innerHTML = renderDetail(state.detail, current); if (response.stale) { const notice = document.createElement("p"); notice.className = "empty-state"; notice.style.padding = "12px 34px"; notice.textContent = "Source sedang lambat; detail terakhir yang tersimpan ditampilkan."; $("#detailBody").prepend(notice); } bindDetail(); showDetail($("#detail")); } catch (error) { $("#detailBody").innerHTML = `<p class="empty-state" style="padding:34px">${error.message}</p>`; } }
function bindDetail() { const list = $("#statusMenu"); $("#statusTrigger").onclick = () => menu(list, !list.classList.contains("is-open")); document.querySelectorAll("[data-status]").forEach((button) => button.onclick = () => { state.status = button.dataset.status; $("#statusLabel").textContent = button.textContent; menu(list, false); }); $("#progressDown").onclick = () => { state.progress = Math.max(0, state.progress - 1); $("#progressValue").textContent = state.progress; }; $("#progressUp").onclick = () => { state.progress = Math.min(state.detail.episodes.length || state.progress + 1, state.progress + 1); $("#progressValue").textContent = state.progress; }; $("#saveTrack").onclick = () => { persistCurrent(); savedFlight($(".detail-poster"), $(".local-badge")); }; document.querySelectorAll("[data-episode]").forEach((button) => button.onclick = () => openPlayer(button.dataset.episode, button.querySelector(".episode-title")?.textContent.trim() || button.textContent.trim(), Number(button.dataset.episodeNumber))); }
function initEnhancements() {
  const cursor = $("#fireCursor");
  if (window.desktopApp?.isDesktop) document.documentElement.classList.add("desktop-shell");
  const pointer = { x: -100, y: -100 };
  const trail = Array.from({ length: 7 }, (_, index) => { const node = document.createElement("i"); node.className = "cursor-particle"; node.style.setProperty("--trail-index", index); cursor?.appendChild(node); return { node, x: pointer.x, y: pointer.y }; });
  if (window.desktopApp?.isDesktop && cursor) { cursor.classList.add("fire-cursor--desktop"); } else if (cursor) { cursor.innerHTML = '<span>&lt;</span><i>~~~~</i>'; }
  window.addEventListener("pointermove", (event) => { if (cursor && event.pointerType !== "touch") { pointer.x = event.clientX; pointer.y = event.clientY; cursor.style.transform = `translate3d(${pointer.x + 7}px, ${pointer.y + 7}px, 0)`; } }, { passive: true });
  const animateCursorTrail = () => { trail.forEach((item, index) => { const follow = index === 0 ? pointer : trail[index - 1]; item.x += (follow.x - item.x) * (index === 0 ? .52 : .24); item.y += (follow.y - item.y) * (index === 0 ? .52 : .24); item.node.style.transform = `translate3d(${item.x - pointer.x - 3}px, ${item.y - pointer.y - 3}px, 0) scale(${1 - index * .09})`; }); requestAnimationFrame(animateCursorTrail); };
  requestAnimationFrame(animateCursorTrail);
  const envDialog = $("#envDialog");
  $("#openEnv").onclick = async () => { try { const current = await fetch("/api/env").then((response) => response.json()); $("#envUrl").value = current.ANIMASU_BASE_URL || ""; $("#envStatus").textContent = ""; envDialog.showModal(); } catch (error) { window.alert(error.message); } };
  $("#cancelEnv").onclick = () => envDialog.close();
  $("#importEnvFile").onclick = () => $("#envFile").click();
  $("#envFile").onchange = (event) => { const file = event.target.files?.[0]; if (!file) return; const reader = new FileReader(); reader.onload = () => { const match = String(reader.result).match(/^\s*ANIMASU_BASE_URL\s*=\s*["']?([^"'\s]+)["']?\s*$/m); if (match) { $("#envUrl").value = match[1]; $("#envStatus").textContent = "URL .env terbaca. Tekan Apply URL."; } else $("#envStatus").textContent = "ANIMASU_BASE_URL tidak ditemukan di file .env."; }; reader.readAsText(file); event.target.value = ""; };
  $("#applyEnv").onclick = async () => { const value = $("#envUrl").value.trim(); $("#envStatus").textContent = "Menerapkan konfigurasi…"; try { const response = await fetch("/api/env", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ANIMASU_BASE_URL: value }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error || "URL tidak valid"); $("#envStatus").textContent = `Aktif: ${data.ANIMASU_BASE_URL}`; $("#sourceStatus").textContent = "ANIMASU / updated"; localStorage.setItem("iln_env", data.ANIMASU_BASE_URL); envDialog.close(); loadDaily(); } catch (error) { $("#envStatus").textContent = error.message; } };
  const toggleAppFullscreen = () => { if (window.desktopApp?.toggleFullscreen) window.desktopApp.toggleFullscreen(); else if (!document.fullscreenElement) document.documentElement.requestFullscreen?.(); else document.exitFullscreen?.(); };
  $("#toggleAppFullscreen").onclick = toggleAppFullscreen;
  window.addEventListener("keydown", (event) => { if (event.key === "F11") { event.preventDefault(); toggleAppFullscreen(); } });
}
async function init() { initEnhancements(); initScene(); initScrollFade(); initPlayer({ onNextEpisode: markWatchedAndGetNext }); renderTracking(); $("#importData").onclick = () => $("#importDataInput").click(); $("#importDataInput").onchange = (event) => { importTrackingFile(event.target.files?.[0]); event.target.value = ""; }; $("#exportData").onclick = downloadTracking; $("#closeDetail").onclick = () => $("#detail").classList.add("hidden"); $("#clearCollection").onclick = () => $("#clearDialog").showModal(); $("#cancelClear").onclick = () => $("#clearDialog").close(); $("#confirmClear").onclick = () => { state.tracking = []; state.collectionFilter = "all"; clearTracking(); renderTracking(); $("#clearDialog").close(); }; $("#clearDialog").addEventListener("click", (event) => { if (event.target === $("#clearDialog")) $("#clearDialog").close(); }); [$("#previousSlide"), $("#previousSlideBottom")].forEach((button) => button.onclick = () => setSlide(state.slide - 1)); [$("#nextSlide"), $("#nextSlideBottom")].forEach((button) => button.onclick = () => setSlide(state.slide + 1)); $("#searchForm").onsubmit = (event) => { event.preventDefault(); state.query = $("#searchInput").value.trim(); search(); }; let debounce; $("#searchInput").oninput = (event) => { state.query = event.target.value.trim(); activateSearch(); clearTimeout(debounce); debounce = setTimeout(search, 300); }; document.querySelectorAll("[data-scroll]").forEach((link) => link.onclick = (event) => { event.preventDefault(); const target = document.querySelector(link.getAttribute("href")); if (target) smoothScroll(target); }); try { const savedEnv = localStorage.getItem("iln_env"); if (savedEnv) await fetch("/api/env", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ANIMASU_BASE_URL: savedEnv }) }); const health = await api.health(); $("#sourceStatus").textContent = `${health.provider.toUpperCase()} / online`; const genres = await api.genres(); $("#genreStrip").insertAdjacentHTML("beforeend", renderGenres(genres.data)); document.querySelectorAll("[data-genre]").forEach((button) => button.onclick = () => { document.querySelector(".genre-chip.is-active")?.classList.remove("is-active"); button.classList.add("is-active"); state.genre = button.dataset.genre; if (state.query || state.genre) { activateSearch(); search(); } else loadDaily(); }); } catch (_) { $("#sourceStatus").textContent = "YAOI / source unavailable"; } loadDaily(); }
init();
